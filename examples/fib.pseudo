func fib(n: int): int
start
    if n == 0 or n == 1 then
        return n;
    else
        return fib(n - 1) + fib(n - 2);
    end
stop

func main(): int
start
    write(fib(10));
    return 0;
stop
